__pycache__/
*.pyc
.DS_Store
Thumbs.db
.venv/

# dist/ is generated by GitHub Actions on every push — not committed.
# (The Random Riot repo does commit dist/; that is because Cloudflare serves
# the directory as-is. Different host, different trade-off.)
dist/
